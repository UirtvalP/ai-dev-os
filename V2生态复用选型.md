# V2 生态复用选型与自研边界

日期：2026-09-06。依据：用户明确“现有的包含世界上所有优秀的东西，不限于我们的代码”。

状态：首轮上游文档与本地接口选型，不是已安装、已验证或已交付声明。Phase 3 尚未通过统一验收；Phase 4–6 仅记录候选，不提前实施。不改写 Phase 0–2 已签发的计划、Receipt、Review 或 Gate。

## 执行原则

1. 先找成熟项目、库、命令行工具、标准协议和已有服务，比较实际契合度，再决定采用、适配或少量自研。不以“项目当前零运行依赖”拒绝合理依赖，也不以 GitHub stars 代替兼容性证据。
2. 优先顺序是直接组合已有能力、使用公开扩展点、编写薄适配层，最后才是确有缺口的独立实现。成熟外部组件和仓库既有实现都接受同一维护成本比较；已写代码不是必须保留的理由。
3. 每项新增通用能力都要记录：候选、上游来源、可直接复用部分、未满足的需求、依赖/许可证/平台限制、选用或不选的原因。引入时再锁定实际版本并做兼容性验收；需要新账户、费用或第三方仓库权限时先取得用户授权。
4. 我们主要维护需求产品语义：Requirement/Task/Agent 的关联、验收事实映射、交付权限、可替换 Provider 的连接与面板体验。不自造 Git 合并算法、终端模拟器、浏览器测试引擎或通用分布式工作流平台。
5. 保留既有 Requirement Workspace、Task Provider、Review Gate、Hook-first 生命周期。外部框架作为替换实现或插件接入，不以选型为由推倒已验收底座。

## 首轮候选和实际处理

| 能力 | 全球现成候选 | 本轮判断与边界 |
| --- | --- | --- |
| Task 分支、worktree、合并、ref CAS | 原生 Git；GitPython、Dulwich、pygit2 | Phase 3 已直接调用 Git，本系统不实现合并算法。比较 Python 库能否减少适配代码；Requirement 租约和验收绑定仍属产品连接层。不能因库存在便认为它提供隔离或跨 Git/Workspace 的交付事务。 |
| 远端 PR 合并队列 | GitHub Merge Queue；Mergify | 优先已有服务，但不能虚报当前可用。实时公开 API 确认本仓库 owner.type=User、public、main；GitHub 官方队列要求组织仓库，因此此仓库当前不满足条件。Mergify 属第三方服务候选，需要单独核对权限和接入，不自动安装或授权。当前 Phase 3 只保留必要本地串行集成与原生 CAS，不发展成通用托管队列。 |
| 多 Agent 流程、暂停恢复 | LangGraph；Temporal | LangGraph 提供 checkpoint、子图和人工中断，是后续 Planning/Orchestration 插件重点候选；Temporal 是持久工作流候选，但需要服务端部署或云服务。两者都不直接提供本项目的 Requirement Gate/主分支交付权限，不能仅换框架就删除这些边界。本轮不重写已通过 Phase 2 的 Supervisor。 |
| 软件 Agent 工具与运行服务 | OpenHands Software Agent SDK；现有 Agent 的公开 SDK/协议、ACP | OpenHands 提供可组合的代码 Agent、工具、工作区、事件与服务接口，应优先评估作为可选 Runtime 后端，不自写通用代码 Agent。ACP 是客户端与编码 Agent 的标准接口，不是“装了桌面应用即已具备该入口”的证明。保留已验收的现有适配器。 |
| 自动测试与证据 | 已有 pytest/Ruff/mypy、GitHub Actions；Playwright | Phase 4 复用真实测试工具、CI 状态与标准报告；Playwright 用于 Dashboard/浏览器端 E2E。我们只做测试策略插件、命令/环境/SHA 绑定和结果转换，不再造测试框架。隔离执行器的依赖可用性必须验证，不能只包装退出码便称端到端可用。 |
| Agent 树、DAG 和终端展示 | React Flow；xterm.js；OpenHands 客户端组件 | Phase 5 优先现成图形与终端组件，面板只做 Requirement/Agent 视图、事件映射、回复和权限；会话事件展示不等同于终端，不能只接 xterm.js 就声称已支持任意 Agent 对话。整套 UI 复用还需单独核对耦合及许可证。 |
| main-only 部署门禁 | GitHub Actions Environments 与分支规则；部署平台公开 API | Phase 6 优先原生环境分支限制、受保护凭据和部署工作流。产品层再核对 main 的确切 SHA、MergeReceipt 和部署结果，不自造部署平台。不在本轮设置仓库规则或部署。 |

## Phase 3 当前实现的处置

- 保留未提交改动，不把“已写完一个文件”当成可用结论，也不为减少代码行数破坏验收链。
- Git worktree/merge-tree/commit-tree/update-ref、现有 Workspace 锁与存储继续作为复用基础。
- 已完成 GitPython/Dulwich/pygit2 首轮对比，并按用户“继续”恢复最小适配：祖先关系、对象批量读取、HTTPS/本地远端查询均复用 Git 原生命令，不自写 Git 传输协议。
- 暂不采用付费或需要额外仓库权限的队列服务；这不是否定服务复用，而是避免未经授权改变仓库管理方式。
- 上轮未完成项已进入本轮整批接线：安全远端读取、main 原生 EOL、accepted 证据显式刷新、已安装 Python 测试依赖、最终 Review/CAS 事务边界均已补实现和相邻回归代码，尚待统一执行证明，不在此宣称通过。
- 下一步是据选型结果完成 Phase 3 整批接线与必要修正，再统一 Review/测试/回归；不在选型或每个模块后零碎执行测试，不提前实施 Phase 4。

## Git 方案比较结论

本轮选型决定：继续直接复用原生 Git，收缩手写 Git 细节；不是将“复用”限制成本仓库代码。

- GitPython 提供 Python 对象接口，但仍依赖 Git，并未消除本项目的安全调用、租约或验收映射；当前仅为包住 subprocess 而新增依赖的收益有限。
- Dulwich 在不依赖 Git 可执行程序时更有价值；当前项目没有这一部署要求，需要额外核对其 worktree/合并语义与用户数据保留规则，不能因接口名称相同就替换。
- pygit2/libgit2 更适合明确需要进程内对象操作的场景；需考虑二进制依赖、平台和传输支持，本轮没有足以支持迁移的性能测量。
- 明确改进方向是把 Git 布局识别、祖先关系、对象批量读取和远端传输尽量交给 Git 官方接口，而不是在 TrustedGit 内重写相同功能。我们只保留路径/权限检查、调用参数约束和业务事实关联。

来源：[GitPython](https://gitpython.readthedocs.io/en/stable/intro.html)、[Dulwich](https://dulwich.readthedocs.io/en/latest/)、[pygit2 安装与平台能力](https://www.pygit2.org/install.html)、[rev-parse](https://git-scm.com/docs/git-rev-parse)、[merge-base](https://git-scm.com/docs/git-merge-base)、[cat-file](https://git-scm.com/docs/git-cat-file)。这是兼容性/维护成本判断，不是实测性能排名。

## 测试并行复用（2026-09-06 第二轮整改）

首轮全量串行 1476.93 秒，第二轮 9 文件相关串行 1114.45 秒；后者最慢用例为真实临时 Git 集成，单项约 13–38 秒。仅降低重复配置调用不足以确保阶段出口每命令 900 秒预算。

采用成熟 pytest-xdist 开发依赖，由 uv.lock 锁定实际版本，项目 pytest 默认固定 `-n 2 --dist=load`。Git/权限域夹具使用独立 tmp_path、UUID 与动态端口，适合先验证两进程并行；不使用按 CPU 自动无限扩张，也不自写测试 runner。不减少测试、不扩大冻结超时、不隐式改 PYTEST_ADDOPTS；改后的配置和锁文件必须作为新候选验证，尚未实测提速或宣称通过。

排查时可显式 `-n 0` 串行；真实在线 Codex smoke 单独以 `-n 0` 执行。若实际出现资源争抢，再按具体证据收敛并发范围，不提前把全部 LPAC/Git 测试串行化。

来源：[pytest-xdist 固定进程数与分发](https://pytest-xdist.readthedocs.io/en/stable/distribution.html)。

## 上游依据

- [Git worktree](https://git-scm.com/docs/git-worktree)、[merge-tree](https://git-scm.com/docs/git-merge-tree)、[update-ref](https://git-scm.com/docs/git-update-ref)
- [GitHub 合并队列与适用范围](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/configuring-pull-request-merges/managing-a-merge-queue)
- [仓库公开事实 API](https://api.github.com/repos/UirtvalP/ai-dev-os)：2026-09-06 本轮只读核对，无鉴权或设置修改。
- [Mergify Merge Queue](https://docs.mergify.com/merge-queue/)
- [LangGraph 持久化](https://docs.langchain.com/oss/python/langgraph/persistence)、[子图](https://docs.langchain.com/oss/python/langgraph/use-subgraphs)
- [Temporal 文档](https://docs.temporal.io/)
- [OpenHands SDK](https://docs.openhands.dev/sdk)、[上游仓库与组件边界](https://github.com/OpenHands/software-agent-sdk)
- [Agent Client Protocol](https://agentclientprotocol.com/get-started/introduction)
- [pytest 输出与报告](https://docs.pytest.org/en/stable/how-to/output.html)、[Playwright CI](https://playwright.dev/docs/ci)
- [React Flow](https://reactflow.dev/)、[xterm.js](https://xtermjs.org/)
- [GitHub 部署环境、分支限制和凭据](https://docs.github.com/en/actions/reference/workflows-and-actions/deployments-and-environments)
