# AI Dev OS V2 实施主计划

状态：已建立，按阶段门禁执行
Requirement：`REQ-020`
开发分支：`feat/REQ-020-v2`
独立工作树：`D:\code\AI Dev OS.worktrees\REQ-020`

## 1. 目标与完成语义

V2 的目标是在保留 V1 持久需求底座的前提下，把 AI Dev OS 增量演进为编辑器无关的多 Agent 软件交付操作系统：用户提交目标后，系统能够选择直接执行或拆成 Task DAG，为每个 Task 选择合适的 Agent Runtime 与模型，隔离 Git 工作区，持续收集对话、工具、验证和 Git 事件，并在全部质量门禁通过后自动合入 `main`。如需部署，只能部署受保护 `main` 的已验证提交。

V2 将“Agent 认为完成”与“系统确认完成”严格分开：

```text
Agent candidate_complete
        ↓
Task Verification Gate
        ↓
Task Accepted
        ↓
V1-compatible Review Packet
        ↓
Requirement Review Gate
        ↓
IntegrationAuthorization
        ↓
Requirement Integration Gate
        ↓
merge → protected main
        ↓
post-merge verification
        ↓
需要部署？── 是 → main HEAD 再确认 → Deployment Gate → DeploymentReceipt
        │                                      ↓
        └──────────────────────────────────────┘
        ↓
RequirementCompletionToken
        ↓
Requirement done / Agent 可向用户汇报完成
```

Worker 和 Primary Agent 都不能直接生成 Review approval、`IntegrationAuthorization` 或 `RequirementCompletionToken`。这些授权只能由确定性 Orchestrator 在结构化事实满足全部适用门禁后生成；Review Gate 沿用 V1 的自动/人工测试策略。若 Requirement 声明部署为验收组成部分，必须先存在对应 `DeploymentReceipt`，未配置部署目标时则在 post-merge verification 后完成。

## 2. 当前仓库审计基线

审计基于 `origin/main` 提交 `1f894ca`。为避免覆盖 `REQ-019` 在主工作树中的未提交修改，`REQ-020` 已使用独立 branch/worktree。

| 能力 | 当前证据 | V2 判断 |
|---|---|---|
| Requirement Workspace | Markdown/JSON、跨进程锁、原子替换、恢复/检查点/交接 | 保留并扩展，不迁移为数据库 |
| Task Provider | `TaskProvider` 与 dashi JSON Adapter，具备 parent、依赖、Session/Git 关联 | 继续作为可替换任务板投影与用户输入边界；执行事实由本地 Orchestrator 持久化 |
| Review Gate | 已检查 Acceptance、Verification、Intent 与开发 Task 状态 | 作为 V2 Requirement Quality Gate 的基础 |
| Hook-first 生命周期 | `SessionStart`、`UserPromptSubmit`、`Stop`、`SessionEnd` | 必须保持入口兼容与幂等 |
| Agent 执行 | Dispatcher 直接依赖同步 `CodexExecProvider` | 缺统一 Runtime、能力发现、控制、消息与事件 |
| 多 Agent 编排 | 单项目、单 write-capable Worker | 缺 Supervisor、DAG、并发槽位、重试与 replan |
| Git 生命周期 | 可读取状态并手工创建 branch/worktree | 缺分配、租约、集成、合并队列和 main 保护 |
| Verification | 固定命令串行执行，首个失败即停 | 缺计划 Provider、提交绑定证据、分层回归和可替换执行器 |
| Event Plane | 仅保存 Session 摘要和执行结束后的原始日志 | 缺统一事件 schema、有序 replay、消息投递和投影视图 |
| Dashboard | 依赖 dashi Task 视图 | 缺自己的 Requirement/Agent/Conversation 控制面 |
| Deployment | 不存在 | 缺 Provider、main-only policy、收据和审计 |

干净基线验证：

- `python -m pytest -q`：`162 passed`
- `python -m ruff check .`：PASS
- `git diff --check`：PASS

`REQ-019` 的 Main/Worker 委派改动与 Phase 1 同时触及 Dispatcher。它必须先独立修复、测试、提交并合入 `main`，随后 V2 分支再更新到该已验收基线。不得把两个 Requirement 的未提交内容混在一次提交中。

本机 Codex CLI `0.141.0` 的真实 Stop Hook smoke 还暴露出既有归档实现的协议缺陷：一次性向 App Server 写完多条请求后立即关闭 stdin，服务端会记录 connection disconnected 并丢弃 `thread/archive` 响应。Phase 1 必须用持续 `stdio` 连接逐请求等待响应，并以真实 archive smoke 关闭该缺陷；不得把 fake runner 通过当作 live 兼容证据。

## 3. 不变原则

1. Requirement First：Requirement Workspace 仍是目标、意图、计划、决策、验收、验证和交接的 Source of Truth。
2. Conversation 是执行可观测数据，不是 Requirement Source of Truth。
3. V1 兼容：既有 Workspace、CLI、Task Provider、Review Gate、Hook 和人类可读文件继续工作。
4. Automation First：确定性状态转换、调度、Git、验证、合并和部署门禁不得交给提示词记忆。
5. Provider 隔离：Core 不依赖 Codex、Cursor、Claude、dashi、GitHub 或具体部署平台。
6. 能力真实声明：Runtime 不得把“结束后检查取消”声明为即时取消，不支持的能力必须返回结构化 `unsupported`。
7. 失败关闭：证据缺失、陈旧、冲突或 Provider 不可用时保持可重试状态，不越过门禁。
8. 可恢复：每个关键状态先持久化再产生外部副作用；重启后通过 reconciliation 收敛。
9. 最小权限：Worker 只获得其 Task、worktree 和策略允许的权限；Dashboard 不绕过审批与 Gate。
10. 不复制第三方实现：只借鉴公开项目的抽象和状态机；复用代码前必须检查许可证并记录来源。

## 4. V2 目标架构

```text
Dashboard / CLI / Task Provider
            │
            ▼
Command Queue + Event Query API
            │
            ▼
Requirement Supervisor ───── Completion Authority
            │
            ├── PlanningPolicy / DecomposerProvider
            ├── ModelRouterProvider
            ├── VerificationPlannerProvider
            └── RecoveryPolicy
            │
            ▼
Deterministic Orchestration Engine
            │
            ├── AgentRuntime Registry
            │      ├── Codex App Server Runtime
            │      ├── Cursor ACP Runtime
            │      └── Claude Runtime
            ├── Git Workspace / Integration Provider
            ├── Verification Provider
            └── Deployment Provider
            │
            ▼
Requirement Workspace + Task Provider + Append-only Event Store
```

### 4.1 核心版本化对象

- `RuntimeDescriptor`：Runtime 身份、版本、能力和可用模型。
- `AgentRunRequest` / `AgentRunResult`：一次 Worker 运行的稳定输入与结果。
- `RuntimeSessionRef`：Provider 无关的 Session/Thread 引用。
- `AgentMessage` / `AgentEvent`：对话和工具执行的标准化观测数据。
- `ExecutionPlan`：`direct` 或带依赖关系的 Task DAG。
- `PolicyDecision`：策略输入摘要、选择、理由、Provider 版本和确定性指纹。
- `WorktreeLease`：Task 对 branch/worktree 的唯一租约。
- `VerificationPlan` / `VerificationReceipt`：测试计划与绑定 commit/environment 的证据。
- `IntegrationCandidate` / `MergeReceipt`：可合并候选与合并事实。
- `PhaseGateRecord`：`schema_version`、Requirement/Phase/Task ID、exact commit SHA、前序 Gate 引用、稳定 Acceptance ID 逐项结果、Verification Receipt 引用、回归摘要、独立 `ReviewAttestation`、签发时间/签发者和最终 PASS/FAIL。
- `RequirementCompletionToken`：只有全部 Requirement Gate 通过后产生。
- `DeploymentRequest` / `DeploymentReceipt`：部署输入、main 证明与结果。

所有持久 schema 都带 `schema_version`；新增数据采用可选目录或字段，旧 Workspace 缺失时使用兼容默认值，不把新目录加入 V1 必需文件集合。

## 5. 阶段门禁协议

每个 Phase 对应一张 Requirement 子 Task，并通过 `blocked_by` 串联。`blocked_by` 是用户可见投影；真正的推进权限来自本地、绑定 exact SHA 的 `PhaseGateRecord`。状态推进规则：

```text
backlog → todo → in_progress → candidate_complete
                         │
                         ├─ Gate FAIL → blocked / 修复后重试
                         └─ Gate PASS → in_review → accepted
```

Task Provider 只支持 `backlog/todo/in_progress/in_review/done/blocked`，因此投影映射固定为：

| 本地域状态 | Task Provider 投影 |
|---|---|
| `planned` / `waiting_dependencies` | `backlog` 或 `blocked` |
| `ready` | `todo` |
| `running` | `in_progress` |
| `candidate_complete` / `verifying` | `in_review` |
| `accepted` | `done` |
| `failed` / `blocked` | `blocked` |

投影状态、Markdown 复选框或 Agent 自述均不能代替 `PhaseGateRecord`。在 Phase 2 的本地 Orchestrator 落地前，由 Phase 0/1 的确定性过渡命令和独立 Reviewer 人工触发相同规则，不得据此放宽门禁。

所有阶段验收项使用稳定 ID（例如 `P1-AC-01`），不得仅靠自由文本匹配。HEAD 改写、rebase、验收项或验证计划变化后，旧 Gate Record 自动失效；后续 Phase 开始时，当前基线必须精确包含前序 Phase 的退出 SHA。Phase 0 先交付最小 Gate Record 生成/校验能力，Phase 4 再扩展为完整 Verification Provider。

执行约束：

1. 前一 Phase 的退出门禁全部 PASS，后一 Phase 才能进入 `in_progress`。
2. 每个 Phase 至少包含契约测试、负向测试、故障恢复测试和 V1 全量回归。
3. 每个 Phase 完成后由未参与实现的 Reviewer 检查边界、错误路径和不必要复杂度。
4. 验证证据必须记录命令、commit、环境、开始/结束时间、退出码和结果摘要。
5. 任一 Gate 失败时更新 Requirement checkpoint 和 Task blocker，不预写下一 Phase 实现。
6. 只有最终 post-merge verification 以及该 Requirement 声明为必需的 Deployment Gate 通过后，才能将 Requirement 标记为 `done`。

统一 `CommonQualityGate` 至少包含：阶段契约测试、负向测试、故障恢复测试、累计 V2 测试、完整 V1 回归、Ruff、真实类型检查、`git diff --check`、构建/安装 smoke 与独立 Review。`ReviewAttestation` 必须记录 Reviewer Session、实现 Run IDs、结论和已解决 findings；Reviewer 不能与实现 Session 相同。

当前任务映射与绑定约束：

| Phase | Task |
|---|---|
| Phase 0 | `AID-151` |
| Phase 1 | `AID-170` |
| Phase 2 | `AID-171` |
| Phase 3 | `AID-172` |
| Phase 4 | `AID-173` |
| Phase 5 | `AID-175` |
| Phase 6 | `AID-174` |

- `AID-150` 是 Requirement Space；`AID-151` 是 Phase 0/bootstrap 控制任务，不代表整个 V2 已完成。
- 执行顺序只以上表、`blocked_by` 和本地 `PhaseGateRecord` 为准，不以 AID 数字顺序为准。
- Phase 0 通过后，`AID-151` 投影为 `done`，当前 Session 与兼容字段 `requirement_task_id` 通过 CAS 切换到 `AID-170`；后续阶段逐一切换，Requirement 本身继续保持 `in_progress`。
- blocked 阶段卡不得预绑定 Runtime Session/worktree；激活时才以 CAS 绑定实际 Session 与隔离 worktree，完成后解绑。
- 新版 Task payload 中顶层 `threadId` 仅是 conversation attribution；只有显式 `threadBinding` 是当前执行绑定。旧 payload 完全缺少 `threadBinding` 字段时才允许兼容回退。

## 6. Phase 0 — 审计与主计划（AID-151）

### 范围

- 建立当前能力矩阵、依赖关系、兼容边界和风险清单。
- 建立 V2 版本化架构与六阶段实施路线。
- 把 `REQ-019` 设为 Runtime 改造的显式前置依赖。
- 为 `REQ-020` 分配独立 branch/worktree，保护脏 `main`。

### 退出标准

- [x] `P0-AC-01` 当前源码、测试、Provider、Hook、Review、Git 和 Dispatcher 已完成证据审计。
- [x] `P0-AC-02` V2 主计划已进入版本控制候选。
- [x] `P0-AC-03` `REQ-020` Requirement、Task、Session 均绑定独立 worktree。
- [x] `P0-AC-04` 干净 `origin/main` 的 pytest、ruff、diff-check 全部通过。
- [x] `P0-AC-05` 六个实施 Task 已创建并以 `blocked_by` 串联，后阶段初始状态均为 `blocked`。
- [ ] `P0-AC-06` Windows/Linux 与 Python 3.11/当前支持版本 CI、真实类型检查均通过。
- [ ] `P0-AC-07` 最小 `PhaseGateRecord` 生成、exact-SHA 校验、失效和前序包含关系有自动测试。
- [ ] `P0-AC-08` `REQ-019` 修复已提交并推送，V2 分支已更新到该基线。
- [ ] `P0-AC-09` CommonQualityGate 与独立 Phase 0 Review 全部通过。

最后一项未通过前，Phase 1 只能做不依赖 Dispatcher 的设计与测试准备，不能完成 Runtime 切换。

## 7. Phase 1 — Agent Runtime V2（AID-170）

### 实现范围

1. 新增 Provider 无关的 Runtime contracts、ports、error 与 capability model。
2. 建立 append-only Runtime Event Store，支持有序追加、幂等 event ID、游标 replay、并发写入和损坏尾行恢复。
3. 实现 Codex App Server `stdio` JSON-RPC Client：初始化、模型发现、thread start/resume/read/archive、turn start/steer/interrupt、事件与服务端请求处理。
4. 实现 Cursor ACP `stdio` JSON-RPC Adapter 与 Claude Agent/CLI 事件 Adapter；三者共享 Runtime contracts，只声明各自真实能力。
5. 将各 Runtime 通知映射为标准 session/turn/message/tool/approval/error/completion 事件；未知事件保真降级。
6. 保留 `CodexAgentProvider` / `CodexExecProvider` 兼容 façade，一个发布周期内不破坏现有 import 和 CLI。
7. Dispatcher 只依赖 `AgentExecutionPort`；具体 Runtime Adapter 仅在 composition root 注入。
8. Runtime 只发布 Session 事实，不重复执行 Hook 已负责的 Requirement/Task/Session attach。

### 退出标准

- [ ] `P1-AC-01` 非 Codex fake Runtime 可完整驱动 Dispatcher。
- [ ] `P1-AC-02` Runtime 能报告真实 capabilities 与 model descriptors；不支持/未安装操作返回结构化结果。
- [ ] `P1-AC-03` Codex start、resume、resume fallback、message、steer、interrupt、archive 与 event replay 契约通过；真实 Stop Hook archive 不再因 stdin 提前关闭而丢响应。
- [ ] `P1-AC-04` Cursor ACP 与 Claude Adapter 的 start/resume/message/control/event 能力矩阵、契约测试和 hermetic E2E 通过。
- [ ] `P1-AC-05` 事件在 Agent 进程退出前可被消费者读取。
- [ ] `P1-AC-06` 旧 Workspace、Session JSON、配置、CLI 和 Python imports 保持兼容。
- [ ] `P1-AC-07` 三种 Adapter 的 fake-server E2E 通过；本机已安装 Runtime 完成 live smoke，未安装 Runtime 明确报告 `unavailable` 而非伪成功。
- [ ] `P1-AC-08` CommonQualityGate 与独立 Phase 1 Review 全部 PASS。

## 8. Phase 2 — Orchestration / Policy Plane（AID-171）

### 实现范围

1. `PlanningPolicy` 输出 `ExecutionPlan(mode=direct|dag)`；Core 不解析自然语言拆解任务。
2. `ModelRouterProvider` 根据 Task 特征和 Runtime capability 选择 Runtime、model、effort、权限配置。
3. `VerificationPlannerProvider` 先生成可审计测试意图；Phase 4 再接完整执行 Provider。
4. `RecoveryPolicy` 对 crash、timeout、provider offline、verification fail 给出 retry/replan/escalate/stop 决策。
5. `RequirementSupervisor` 是单写者；通过 lease、版本号和 reconciliation 管理 Worker、并发槽位、依赖与重启恢复。
6. Worker 只能产生 `candidate_complete`、`blocked` 或 `failed`，不能直接完成 Task/Requirement。
7. 默认策略为可单测的本地规则实现；以后可替换为插件或 Agent 策略，但策略输出必须结构化并持久化。
8. 冻结最小 `VerificationPlan`、`VerificationReceiptEnvelope` 与 `VerificationExecutorPort`，供 Phase 3 在没有完整 Provider 前生成结构化、新鲜的合并证据。

### 退出标准

- [ ] `P2-AC-01` direct 与 DAG 两条路径都有 E2E。
- [ ] `P2-AC-02` DAG 循环、未知依赖、重复 dispatch、并发超限和租约过期均被拒绝或收敛。
- [ ] `P2-AC-03` 模型路由只选择 Runtime 实际报告可用的模型和能力。
- [ ] `P2-AC-04` Worker crash/restart、retry budget、replan 和人工阻塞都有确定性测试。
- [ ] `P2-AC-05` Policy Provider 可替换，Core 测试使用至少两个 fake 实现。
- [ ] `P2-AC-06` Task Provider 离线时本地执行不丢失，恢复后投影幂等收敛。
- [ ] `P2-AC-07` CommonQualityGate 与独立 Phase 2 Review 全部 PASS。

## 9. Phase 3 — Git Integration / Merge Gate（AID-172）

### 实现范围

1. 新增 `GitWorkspaceProvider`，幂等创建/恢复/释放每个 Task 的 branch、worktree 与 lease。
2. 新增 `IntegrationProvider` 与 Requirement integration branch；Worker 不直接写 `main`。
3. 建立 merge queue：检查候选 commit、基线、工作树清洁度、冲突、Task Gate、Requirement Gate 和证据新鲜度。
4. 合并前运行 integration verification；合并后记录 `MergeReceipt` 并运行 post-merge verification。
5. 失败时保留可诊断 worktree/branch，不自动删除用户改动；恢复操作幂等。
6. `main` 存在未提交修改、HEAD 漂移或远端落后时拒绝集成。
7. 使用 `LegacyVerificationAdapter` 包装现有执行器，生成绑定 candidate SHA/tree、环境、命令列表哈希与结果的最小 Receipt；不得从 Markdown 展示文本反推事实。Phase 4 在不改变 Merge Gate 接口的前提下替换完整 Provider并重跑本阶段测试。

### 退出标准

- [ ] `P3-AC-01` 并行 Task 永不共享 branch/worktree；重启后租约可恢复。
- [ ] `P3-AC-02` 未验证、陈旧证据、冲突、脏 main、非预期 base 和重复 merge 都有负向测试。
- [ ] `P3-AC-03` 只有全部 Task accepted 且 Requirement Review Gate 通过后才签发 `IntegrationAuthorization`。
- [ ] `P3-AC-04` 同一 merge request 重试只产生一个可识别结果；ref update 前后崩溃均可 reconcile。
- [ ] `P3-AC-05` merge 副作用前重新解析 expected main SHA，授权后的 ref 漂移会失败关闭。
- [ ] `P3-AC-06` post-merge 失败不会生成 CompletionToken 或触发部署。
- [ ] `P3-AC-07` CommonQualityGate 与独立 Phase 3 Review 全部 PASS。

## 10. Phase 4 — Verification Provider（AID-173）

### 实现范围

1. 把固定命令执行拆成 `VerificationPlannerProvider` 与 `VerificationExecutorProvider`。
2. 默认本地 Provider 兼容当前 `pyproject.toml` 命令，并增加 suite、timeout、working directory、环境白名单和 artifact 约束。
3. `VerificationReceipt` 绑定 plan fingerprint、commit SHA、环境摘要、耗时、退出码、结果摘要和 artifact digest。
4. 支持 unit/type/lint/integration/e2e/security/custom suite；名称与实际工具一致，不再把 lint 冒充 type check。
5. 支持 fail-fast 与 collect-all 策略，但 Gate 明确要求的 suite 缺失时必须失败。
6. 证据变化、commit 变化或环境不匹配时自动判定旧 Receipt 失效。

### 退出标准

- [ ] `P4-AC-01` Provider 替换不修改 Core；fake/local 两类实现通过同一 contract suite。
- [ ] `P4-AC-02` timeout、进程不可用、输出限长、artifact 缺失、陈旧 commit 与部分通过均有测试。
- [ ] `P4-AC-03` Phase Gate 能声明必需 suites，未执行或失败时不得推进。
- [ ] `P4-AC-04` Review Packet 使用结构化 Receipt，不再从展示文本反推验证事实。
- [ ] `P4-AC-05` Phase 3 的 Legacy Receipt 兼容迁移及全部 Merge Gate 回归通过。
- [ ] `P4-AC-06` CommonQualityGate 与独立 Phase 4 Review 全部 PASS。

## 11. Phase 5 — Dashboard / Event Plane（AID-175）

### 实现范围

1. 以 Requirement 为首页对象，展示 Task DAG、Primary/Worker/Test Agent 树、Runtime/model、状态、Git、验证和阻塞。
2. Event Query API 从 append-only Event Store 构建可重放投影；Dashboard 缓存可重建，不成为事实来源。
3. Agent 详情展示规范化消息、工具调用、文件变化、验证、审批、成本/用量（Runtime 提供时）和当前 turn。
4. 支持向指定 Agent：运行中 `steer`、空闲时开始新 turn、不支持即时投递时写入持久 command queue。
5. 每条用户指令具有 command ID、目标 Session、状态、投递时间和结果；重试不重复发送。
6. 提供本地认证/来源检查、输入限长和危险操作的服务端 Gate；前端不能直接调用 Runtime 或 Git。
7. dashi 继续作为 Task Provider；V2 Dashboard 聚合它而不是强制替换。

### 退出标准

- [ ] `P5-AC-01` 页面可展开 Requirement → Agent → Session/Turn，并实时看到事件增量。
- [ ] `P5-AC-02` 运行中回复、排队下一条、取消和失败重试都有 API/浏览器 E2E。
- [ ] `P5-AC-03` 页面刷新、服务重启和投影重建不丢消息、不重复投递。
- [ ] `P5-AC-04` Dashboard 关闭时 Orchestrator、Workspace 和 Hook 生命周期仍完整工作。
- [ ] `P5-AC-05` 本地来源限制、权限、脱敏、游标、断线重连与跨 Requirement 隔离有负向测试。
- [ ] `P5-AC-06` CommonQualityGate、可访问性 smoke 与独立 Phase 5 Review 全部 PASS。

## 12. Phase 6 — main-only Deployment Gate（AID-174）

### 实现范围

1. 新增 `DeploymentProvider`、`DeploymentPolicy`、`DeploymentReceipt` 与环境注册表。
2. 默认硬门禁要求：目标 commit 等于本地受保护 `refs/heads/main` 的解析 HEAD，并按项目策略确认远端 main 一致。
3. 部署前要求当前 commit 的 `MergeReceipt`、post-merge `VerificationReceipt` 与 `DeploymentAuthorization` 均有效；此时不得要求尚未签发的 `RequirementCompletionToken`。
4. 部署命令由 Provider 执行；Core 不包含平台脚本、密钥或环境特定逻辑。
5. 幂等键为 project/environment/commit/provider version；重复请求返回同一收据或明确状态。
6. 保存发起者、目标环境、main 证明、验证证据、开始/结束时间、结果和回滚指引。

### 退出标准

- [ ] `P6-AC-01` feature、integration、detached HEAD、旧 main commit、脏/漂移 main 均被拒绝。
- [ ] `P6-AC-02` 缺失或陈旧 Merge/post-merge Verification/Deployment Authorization 时拒绝部署。
- [ ] `P6-AC-03` 成功、失败、超时、重复请求和 Provider 离线均有测试和可恢复状态。
- [ ] `P6-AC-04` 只有 main-only Gate 能调用实际 Deployment Provider；副作用前再次解析 main，TOCTOU 漂移时拒绝。
- [ ] `P6-AC-05` 无需部署与 deployment-required 两条完整 E2E 均正确签发 CompletionToken。
- [ ] `P6-AC-06` CommonQualityGate、真实本地 dry-run E2E 与独立安全 Review 全部 PASS。

## 13. 跨阶段风险与处理

| 风险 | 处理 |
|---|---|
| `REQ-019` 与 Runtime 同改 Dispatcher | 先独立合入 REQ-019，再更新 V2；禁止复制脏改动 |
| Codex WebSocket transport 仍属实验 | 本地 V2 首选官方 `stdio` JSONL；传输层单独适配 |
| Codex 0.141 一次性 stdin 归档丢响应 | Runtime 维持长连接，initialize 后逐请求等待对应 response；真实 Stop Hook smoke 纳入 Phase 1 Gate |
| Provider 能力不对齐 | capability negotiation + 结构化 unsupported/deferred |
| JSONL 长期增长 | V2 先按 Requirement/run 分片并可重建投影；压缩与归档按实测再引入 |
| 多进程重复执行 | 单写 Supervisor、lease、CAS、幂等键、启动 reconciliation |
| Windows 进程树取消困难 | Runtime 保存受控 process handle，测试真实 child-tree 终止与清理 |
| 自动 merge 破坏用户工作 | 只操作隔离 worktree；脏 main/漂移/冲突一律 fail closed |
| Dashboard 成为第二事实来源 | UI 只发 command、读 projection；Domain State 仍在 Workspace/Task/Git |
| 测试数量增长导致反馈慢 | 维护 contract、phase、full-regression 三层 suite，退出 Gate 仍跑全量 |
| 外部 Agent/模型接口变化 | Adapter 版本与 capabilities 入证据；Core contract 版本化 |

## 14. V1 → V2 迁移与回滚

V2 采用只追加、可忽略的新目录和版本化 schema，不破坏或重写 V1 必需文件；读取和重写持久数据时保留未知字段。V1 与 V2 Dispatcher 不得双写，同一项目任何时刻只有一个调度 owner。启用 V2 前先排空运行、保存 Workspace/Task binding 快照并执行 dry-run；切回 V1 前必须确认无活动 lease/Run，由 V1 忽略 V2 扩展状态。

迁移测试至少覆盖：旧 Workspace 直接读取、旧数据升级、未知字段保留、原子写入中断恢复、新代码写入后旧 V1 读取核心状态，以及 V1/V2 owner 切换不重复 dispatch。持久 schema 采用 forward-compatible 策略；回滚代码不做破坏性降级。

`main` 合并后不得 reset 或 force-push。post-merge 验证失败时进入 `recovery_required`，保留 worktree/receipts 并生成可审计 `RevertPlan`。Deployment Provider 必须声明 rollback capability；目标环境若无回滚能力，需显式策略豁免。部署回滚使用原 `DeploymentReceipt` 作为幂等输入并生成 `RollbackReceipt`。

## 15. 参考边界

- OpenAI Codex App Server：用于 Codex 的 rich client、conversation history、approvals 和 streamed events；本地首选 `stdio` JSONL。
  <https://developers.openai.com/codex/app-server/>
- OpenAI Symphony：借鉴单一 Orchestrator、隔离 workspace、claim/retry/reconciliation；不复制其 Issue-first 主模型。
  <https://github.com/openai/symphony/blob/main/SPEC.md>
- Cursor ACP：用于 Phase 1 Cursor Runtime Adapter 的 `stdio` JSON-RPC、session/update 和 permission boundary。
  <https://cursor.com/docs/cli/acp>
- Claude Managed Agents / Agent SDK：用于 Phase 1 Claude Runtime Adapter 的 session、event stream 和 interrupt/steer 语义参考。
  <https://platform.claude.com/docs/en/managed-agents/events-and-streaming>

第三方项目只提供接口与状态机参考；AI Dev OS 保持 Requirement-first、Provider-independent 和 local-first。

## 16. 当前下一步

1. 等待并验收 `REQ-019` 的取消/状态查询修复、全量测试、提交与推送。
2. 完成 CI、Python 版本矩阵与真实类型检查，关闭 Phase 0 的质量基础设施缺口。
3. 将 `feat/REQ-020-v2` 更新到新的 `origin/main`，重新验证干净基线。
4. Phase 0 全部退出标准通过后，解除 AID-170 的阻塞并只实现 Agent Runtime V2；Phase 1 退出门禁未通过前不写 Phase 2。
