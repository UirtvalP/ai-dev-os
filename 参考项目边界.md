# 参考项目与借鉴边界

本项目不是对任何现有项目的复刻。

目标是吸收不同项目中成熟的抽象，同时避免继承它们不适合本项目目标的复杂度。

## 1. dashi-taskboard

### 参考什么

重点参考：

- Issue / Task 生命周期
- Task 与 Codex Thread 的关联
- Task 与 Git branch / worktree 的关联
- parent / blocks / blocked_by / related 等任务关系
- local-first 思路
- CLI-first 思路
- 用户确认后才允许任务真正完成的 Human Approval Gate

可借鉴的数据模型：

```text
Task
├── status
├── priority
├── parent
├── dependencies
├── branch
├── worktree
└── sessions
```

### 不抄什么

不要把 dashi 的 Issue 直接等同于 Requirement。

本项目必须保留：

```text
Requirement
    ↓
Task[]
```

而不是：

```text
Issue = Requirement = Task
```

不要把大量需求上下文、架构决策、执行历史都塞进 Task comment。

dashi 在本项目中主要作为：

```text
Task State Provider
```

而不是整个 Workspace 的 Source of Truth。

---

# 2. Multica

### 参考什么

重点参考：

- Agent abstraction
- Runtime abstraction
- 多 Agent 统一管理
- Agent 与任务分配关系
- 不同 coding agent 使用统一接口
- 本地 Runtime / Remote Runtime 解耦

未来 Agent Provider 可以参考这种结构：

```text
AgentProvider
├── Codex
├── Claude Code
├── Cursor
├── OpenCode
└── Multica
```

### 不抄什么

V1 不实现：

- Agent fleet
- 多 Agent 调度
- Agent dashboard
- Remote Runtime
- 自动任务分发
- Agent organization / team

不要让整个系统以 Agent 为中心。

本项目真正的一级对象是：

```text
Requirement Workspace
```

而不是 Agent。

---

# 3. Agent Orchestrator

### 参考什么

重点参考 Execution Model：

```text
Task
↓
Workspace
↓
Worktree
↓
Agent Session
↓
Code
↓
Verify
↓
PR
↓
Review
```

尤其参考它对不同基础设施进行 Adapter / Plugin 抽象的方式。

建议借鉴以下接口边界：

```text
Agent Provider
Task Provider
Git Provider
Workspace Provider
SCM Provider
Runtime Provider
```

还可以参考：

- 一个 Task 对应独立 Worktree
- Session 可以恢复
- CI / Review feedback 返回原 Agent
- Execution 与 Issue Tracker 解耦

### 不抄什么

V1 不实现：

- 自动创建 PR
- 自动 merge
- CI feedback loop
- GitHub lifecycle automation
- tmux/process orchestration
- 自动 spawn 多 Agent

不要让：

```text
Issue → Agent → PR
```

成为系统主模型。

本项目主模型仍然是：

```text
Requirement
↓
Task
↓
Session
```

---

# 4. Maestro

### 参考什么

主要参考 UI / UX，而不是核心数据模型。

未来 Dashboard 可以借鉴：

```text
Project
├── Workspace
├── Tasks
├── Agents
├── Sessions
├── Git
└── Timeline
```

参考：

- 多 Workspace 可视化
- Agent 状态可视化
- 独立 Session History
- Worktree 状态展示
- Control Center 思路
- Playbook 的可视化入口

### 不抄什么

V1 完全不做 Maestro 式桌面应用。

不要：

- Electron 优先
- UI-first
- 把 Session 列表当核心组织结构
- 把 Prompt / Conversation 当 Workspace

GUI 只能是：

```text
Workspace 的视图
```

不能成为：

```text
Workspace 本身
```

---

# 5. Superpowers

### 参考什么

重点参考：

- Skill 驱动开发流程
- brainstorming / planning / implementation / review 等阶段拆分
- Agent 在执行前知道当前处于什么开发阶段
- 软件工程流程可以通过 Skill 表达
- 不同阶段使用不同执行策略

特别值得参考：

```text
Workflow as Instructions
```

即：

```text
Skill
↓
约束 Agent 行为
```

### 不抄什么

这是本项目尤其要避免的部分。

不要照搬：

```text
任何任务
↓
Brainstorm
↓
Design
↓
Plan
↓
Implement
↓
Review
```

禁止小 Bug 强制走完整流程。

不要：

- 为流程而创建计划
- 明显问题仍要求长篇分析
- 每个修改都 brainstorm
- 每个任务都生成设计文档
- 轻量任务使用多个 subagent
- 流程步骤固定不可跳过

本项目采用：

```text
Task Complexity Router
```

原则：

```text
默认最轻流程
证据证明复杂后才升级
```

支持：

```text
tiny
normal
complex
research
```

例如：

```text
Tiny:
inspect → fix → verify
```

而不是完整 Superpowers 流程。

---

# 6. Everything Claude Code

### 参考什么

重点参考：

- checkpoint
- session lifecycle
- context persistence
- session summary
- handoff
- continuous learning
- reusable skills
- reusable rules
- subagent organization

尤其参考：

```text
Session 可以结束
Memory 必须留下
```

本项目进一步明确为：

```text
Session is disposable.
Workspace is persistent.
```

### 不抄什么

不要把所有 Session 信息都长期保存。

不要：

- 保存完整 Conversation 作为长期 Memory
- 自动把所有经验升级成 Skill
- 无筛选进行 Continuous Learning
- 每一次工作都生成大量 Memory
- 把 Memory 系统和 Agent Runtime 强绑定

V1 只保留：

```text
state
plan
decisions
verification
handoff
```

---

# 7. Obsidian

### 参考什么

Obsidian 主要作为未来的 Knowledge Provider。

参考：

- Markdown-first
- Local-first
- 双向链接
- 跨项目知识关联
- 人类可直接编辑
- 长期知识网络

未来可以实现：

```text
KnowledgeProvider
├── Markdown
└── Obsidian
```

适合沉淀：

- Architecture patterns
- Technical decisions
- Root causes
- Known pitfalls
- Reusable solutions
- Project conventions
- Deployment knowledge

### 不抄什么

不要把 Taskboard 做进 Obsidian。

不要把：

```text
任务状态
Agent 状态
Session 状态
Git 状态
```

都当笔记管理。

不要自动把所有 Handoff 写进 Obsidian。

只有真正可跨任务 / 跨项目复用的信息才进入 Knowledge。

---

# 8. GitHub Issues / Linear

### 参考什么

参考成熟的：

- Issue 状态
- Priority
- Label
- Relationship
- Human collaboration
- External tracker integration

未来作为：

```text
TaskProvider
```

实现。

### 不抄什么

不要让 GitHub Issue 成为 Workspace 数据库。

不要依赖：

```text
Issue Description
```

保存完整 Requirement Context。

外部 Tracker 可以被替换。

Workspace 必须仍然能够独立存在。

---

# 9. 本项目真正原创的组合

上述项目各自解决一部分问题。

本项目组合为：

```text
Requirement Workspace
        ↓
Adaptive Workflow
        ↓
Task Graph
        ↓
Agent Session
        ↓
Git Execution
        ↓
Checkpoint / Handoff
        ↓
Knowledge Extraction
```

核心差异不是单个功能，而是以下几个原则同时成立：

## Requirement First

Requirement 是一级持久对象。

## Task != Requirement

Task 是 Requirement 的执行分解。

## Session != Context

Session 是临时执行环境。

## Workspace = Source of Truth

跨 Session 状态由 Workspace 保存。

## Adaptive Workflow

流程复杂度跟随任务复杂度。

## Knowledge != History

知识不是历史记录，而是经过筛选的可复用经验。

---

# 10. 实现时的抄袭边界

允许：

- 学习公开项目架构
- 学习数据模型
- 学习模块边界
- 学习交互方式
- 学习工作流思想
- 使用允许复用的开源依赖

禁止：

- 未检查许可证直接复制源代码
- 大段复制实现
- 复制项目 branding
- 复制 README 文案
- 复制独有 UI 设计
- 把别人的代码换名字后提交

如果确实需要复用代码：

1. 先检查 License。
2. 明确记录来源。
3. 满足 Attribution 要求。
4. 优先重新实现抽象，而不是复制实现。

原则：

```text
Copy ideas.
Reuse abstractions.
Reimplement behavior.
Do not blindly copy code.
```

---

# 11. 给 Codex 的参考优先级

如果实现过程中需要参考现有项目，按以下顺序：

## Workspace / Thread / Task

优先看：

```text
dashi-taskboard
```

## Agent / Runtime abstraction

优先看：

```text
Multica
Agent Orchestrator
```

## Workflow / Skill

优先看：

```text
Superpowers
```

但只能学习阶段化思想，禁止照搬重型流程。

## Session / Handoff / Learning

优先看：

```text
Everything Claude Code
```

## Future Dashboard

优先看：

```text
Maestro
```

## Knowledge

优先看：

```text
Obsidian
```

---

# 12. 最终原则

不要试图把这些项目拼接起来。

应该：

```text
理解它们为什么这么设计
        ↓
提取正确 abstraction
        ↓
重新组合
        ↓
形成自己的统一模型
```

本项目不是：

```text
Multica
+ dashi
+ Superpowers
+ Obsidian
```

简单拼盘。

而应该形成统一对象关系：

```text
Project
└── Requirement
    ├── Workspace Memory
    ├── Task Graph
    │   ├── Task A
    │   └── Task B
    ├── Sessions
    │   ├── Codex Session
    │   └── Future Agent Session
    ├── Git Context
    └── Knowledge Candidates
```

外部系统全部通过 Adapter 接入。

Core 永远不应该依赖某一个具体外部项目。