# AI 工作区编排器 V1 架构

文中保留的英文主要是代码、命令、字段名或产品名。常用术语对照：Requirement（需求）、
Task（任务）、Workspace（工作区）、Session / Thread（会话 / 线程）、Workflow（工作流）、
Checkpoint（检查点）、Handoff（交接）、Context Snapshot（上下文快照）、Adapter（适配器）、
Provider（提供方）、Source of Truth（事实来源）、branch（分支）、worktree（工作树）。
程序状态值对照：`draft`（草稿）、`ready`（就绪）、`in_progress`（进行中）、
`in_review`（审查中）、`done`（已完成）、`blocked`（已阻塞）。

## 1. 项目定位

本项目是一个面向 AI Coding Agent 的持久工作区编排系统。

它不替代 Codex、Claude Code、Cursor、dashi-taskboard 或 Git，而是在这些工具之上增加一层统一的 Workspace / Requirement / Workflow 管理。

核心目标：

1. 一个需求拥有独立、持久的 Workspace。
2. 一个需求可以跨越多个 Agent Session / Thread。
3. Thread 只是临时执行上下文，不是需求的 Source of Truth。
4. Agent 每次进入一个需求时，都可以自动恢复必要上下文。
5. 根据任务复杂度自动选择轻量或完整工作流。
6. 任务状态可以同步到 dashi-taskboard。
7. Git branch / worktree 与任务关联。
8. 为未来接入 Multica、多 Agent、Obsidian 留出 Adapter 接口。
9. V1 优先保证简单、快速、可靠，不实现复杂自治 Agent 系统。

---

# 2. 核心设计原则

## 2.1 工作区优先（Workspace First）

不要把 Conversation / Thread 当成项目上下文。

正确模型：

```text
Requirement Workspace
        ↓
Persistent State
        ↓
Task Graph
        ↓
Session / Thread
```

而不是：

```text
Thread
  ↓
所有需求上下文
```

Thread 可以随时结束、替换。

Workspace 必须长期存在。

---

## 2.2 会话可替换（Sessions Are Disposable）

每一个 Codex Thread 都只是一个 Session。

一个 Requirement 可以关联任意数量 Session：

```text
REQ-001
├── 会话 A
├── 会话 B
├── 会话 C
└── 会话 D
```

新 Session 不依赖旧 Session 完整聊天历史。

它只通过 Workspace Snapshot 恢复上下文。

---

## 2.3 事实来源（Source of Truth）必须明确

不同信息分别由不同系统负责。

### 工作区（Workspace）

负责：

- 需求（Requirement）
- 验收标准（Acceptance Criteria）
- 当前阶段
- 计划（Plan）
- 架构决策（Architecture Decisions）
- 交接（Handoff）
- 验证（Verification）
- 上下文快照（Context Snapshot）

### dashi-taskboard

负责：

- 任务（Task）
- 状态（Status）
- 优先级（Priority）
- 父任务 / 子任务（Parent / child）
- 阻塞关系（`blocks` / `blocked_by`）
- 会话 / 线程 ID（Session / Thread ID）
- Git 分支（branch）
- Git 工作树（worktree）

### Git

负责：

- 代码状态（Code State）
- 提交（Commit）
- 分支（Branch）
- 差异（Diff）

### Agent（智能体）

负责：

- 当前执行

Agent Conversation 不属于 Source of Truth。

---

# 3. V1 总体架构

```text
┌────────────────────────────────────┐
│            命令行 / Skill          │
│                                    │
│ workspace new                      │
│ workspace run                      │
│ workspace resume                   │
│ workspace status                   │
│ workspace checkpoint               │
│ workspace handoff                  │
│ workspace review                   │
└──────────────────┬─────────────────┘
                   │
                   ▼
┌────────────────────────────────────┐
│              工作区编排器           │
│                                    │
│ - 需求管理器                        │
│ - 复杂度路由器                      │
│ - 工作流引擎                        │
│ - 上下文恢复                        │
│ - 检查点管理器                      │
│ - 交接管理器                        │
└──────────────┬──────────────┬──────┘
               │              │
               ▼              ▼
┌──────────────────┐   ┌──────────────────┐
│    任务适配器     │   │    Git 适配器    │
│                  │   │                  │
│ dashi-taskboard  │   │ branch（分支）    │
│                  │   │ worktree（工作树）│
└──────────────────┘   │ status（状态）    │
                       └──────────────────┘
               │
               ▼
┌────────────────────────────────────┐
│            Agent 适配器             │
│                                    │
│ V1：Codex                          │
│ 后续：Claude / Cursor / Multica    │
└────────────────────────────────────┘
```

---

# 4. 推荐项目目录

```text
ai-workspace/
├── README.md
├── AGENTS.md
├── pyproject.toml
│
├── src/
│   └── workspace_orchestrator/
│       ├── cli/
│       │   ├── main.py
│       │   └── commands/
│       │       ├── new.py
│       │       ├── run.py
│       │       ├── resume.py
│       │       ├── status.py
│       │       ├── checkpoint.py
│       │       ├── handoff.py
│       │       └── review.py
│       │
│       ├── core/
│       │   ├── models.py
│       │   ├── requirement.py
│       │   ├── workspace.py
│       │   ├── task.py
│       │   ├── session.py
│       │   └── snapshot.py
│       │
│       ├── workflow/
│       │   ├── router.py
│       │   ├── engine.py
│       │   ├── tiny.py
│       │   ├── normal.py
│       │   ├── complex.py
│       │   └── research.py
│       │
│       ├── context/
│       │   ├── restore.py
│       │   ├── checkpoint.py
│       │   └── handoff.py
│       │
│       ├── adapters/
│       │   ├── base.py
│       │   ├── task/
│       │   │   ├── base.py
│       │   │   └── dashi.py
│       │   ├── agent/
│       │   │   ├── base.py
│       │   │   └── codex.py
│       │   └── git/
│       │       ├── base.py
│       │       └── local_git.py
│       │
│       ├── services/
│       │   ├── complexity.py
│       │   ├── verification.py
│       │   └── lifecycle.py
│       │
│       └── utils/
│
├── templates/
│   └── workspace/
│       ├── requirement.md
│       ├── state.md
│       ├── plan.md
│       ├── decisions.md
│       ├── verification.md
│       └── handoff.md
│
├── skills/
│   └── workspace-orchestrator/
│       └── SKILL.md
│
└── tests/
```

V1 推荐 Python 实现。

原因：

- CLI 开发快。
- 文件系统处理简单。
- subprocess 调 Git / taskctl 简单。
- 后续接 API / daemon 方便。
- V1 不需要复杂前端。

---

# 5. 工作区文件结构

每一个 Requirement 创建独立目录。

例如：

```text
.workspace/
└── REQ-001/
    ├── meta.json
    ├── requirement.md
    ├── state.md
    ├── plan.md
    ├── decisions.md
    ├── verification.md
    ├── handoff.md
    └── sessions.json
```

---

# 6. meta.json

保存机器可读元数据。

例如：

```json
{
  "id": "REQ-001",
  "title": "实现身份认证系统",
  "status": "in_progress",
  "complexity": "normal",
  "workflow": "normal",
  "created_at": "2026-08-29T00:00:00",
  "updated_at": "2026-08-29T00:00:00",
  "task_provider": "dashi",
  "agent_provider": "codex",
  "git": {
    "branch": "feature/REQ-001-auth",
    "worktree": ".worktrees/REQ-001"
  }
}
```

meta.json 不保存长文本上下文。

长文本放 Markdown。

---

# 7. requirement.md

这是 Requirement 的稳定定义。

格式：

```markdown
# 需求

## 目标

实现用户登录认证。

## 背景

当前系统没有统一认证机制。

## 范围

- 登录
- Token 生成
- Token 校验

## 非目标

- OAuth
- 第三方登录

## 验收标准

- [ ] 正确账号密码可以登录
- [ ] 错误密码返回 401
- [ ] Token 可以访问受保护 API
- [ ] 过期 Token 无法访问
```

规则：

需求一旦进入执行阶段，不允许 Agent 随意改变目标或范围（`Goal` / `Scope`）。

如果用户需求发生变化，应记录变更，而不是静默改写历史需求。

---

# 8. state.md

保存当前实时状态。

格式：

```markdown
# 状态

## 阶段

implementation（实现）

## 已完成

- 用户模型
- 密码哈希

## 进行中

- 登录接口

## 待处理

- 认证中间件
- 集成测试

## 已阻塞

无

## 下一步行动

实现登录接口校验。
```

这是 Restore 时最重要的文件之一。

---

# 9. plan.md

保存当前任务计划。

格式：

```markdown
# 计划

- [x] 检查现有用户模型
- [x] 添加密码校验
- [-] 实现登录接口
- [ ] 添加认证中间件
- [ ] 添加测试
```

规则：

微型任务可以不创建详细计划。

常规任务使用简短计划。

只有复杂任务才允许使用详细任务图。

---

# 10. decisions.md

记录重要决策。

格式：

```markdown
# 决策

## DEC-001

日期：2026-08-29

决策：

使用 JWT 进行身份认证。

原因：

- 现有后端无状态
- 没有服务端会话基础设施

未采用的方案：

- 服务端会话
- OAuth
```

只记录真正影响未来工作的决策。

不要记录：

```text
修改 foo.py 第 23 行
```

---

# 11. verification.md

保存验证状态。

```markdown
# 验证

## 单元测试

命令：

pytest tests/auth

状态：

PASS

## 类型检查

状态：

PASS

## 集成测试

状态：

TODO
```

需求是否可以进入审查，必须参考验证结果。

---

# 12. handoff.md

这是 V1 最核心文件之一。

每次 Session 结束时更新。

例如：

```markdown
# 交接

## 上次会话

会话 ID：

codex-thread-xxx

## 已完成

- 已实现登录接口
- 已添加密码校验

## 已修改文件

- src/auth/login.py
- tests/test_login.py

## 当前状态

登录接口可以正常工作。

认证中间件尚未实现。

## 重要上下文

不要把令牌校验移入 `UserService`。

认证逻辑应保留在 `auth` 模块中。

## 建议的下一步行动

实现认证中间件。

## 已知问题

无。
```

新的 Thread 启动后，应该优先读取 handoff，而不是依赖历史 Conversation。

---

# 13. 会话模型

sessions.json：

```json
[
  {
    "id": "thread-001",
    "agent": "codex",
    "started_at": "...",
    "ended_at": "...",
    "task_ids": [
      "TASK-001"
    ],
    "result": "completed"
  }
]
```

Session 只负责记录：

- 哪个 Agent
- 哪个 Thread
- 执行过哪些 Task
- 什么时候开始/结束
- 最终结果

不要把完整聊天记录存进去。

---

# 14. 复杂度路由器

这是为了避免 Superpowers 式过度思考。

支持：

```text
tiny
normal
complex
research
```

默认：

```text
normal
```

但明显简单任务应自动降级为 tiny。

---

# 15. 微型工作流（tiny）

适合：

- 小 Bug
- 简单 UI 修改
- 文案
- 明确的空指针
- 明确的条件判断错误
- 小范围配置修改

流程：

```text
inspect
↓
fix
↓
verify
↓
checkpoint
```

禁止：

- 长篇 brainstorming
- architecture design
- 创建复杂 task graph
- 大范围代码扫描
- 无必要的 alternative analysis

Agent 指令：

```text
Prefer execution over planning.

Only inspect code directly related to the issue.

Do not create a multi-stage design unless the issue proves broader than expected.
```

如果发现影响范围扩大：

```text
tiny → normal
```

---

# 16. 常规工作流（normal）

适合绝大多数日常开发任务。

流程：

```text
restore
↓
understand
↓
plan-lite
↓
implement
↓
verify
↓
checkpoint
↓
handoff / review
```

Plan 最多保持简短。

例如：

```text
1. inspect auth module
2. implement middleware
3. add tests
```

不需要写几百行设计文档。

---

# 17. 复杂工作流（complex）

只有满足以下条件之一才进入：

- 跨多个模块
- 数据库 schema 变化
- API contract 大改
- 新系统
- 架构调整
- 高风险功能
- 多个 Task 存在依赖关系
- 用户明确要求设计

流程：

```text
restore
↓
requirement analysis
↓
design
↓
task graph
↓
implementation
↓
integration
↓
verification
↓
review
```

Complex Workflow 可以允许更深分析。

但仍然必须避免无意义反复推演。

---

# 18. 研究工作流（research）

适合：

- 技术选型
- Debug root cause 未知
- 性能调查
- 架构调研

流程：

```text
question
↓
investigate
↓
evidence
↓
compare
↓
decision
↓
knowledge candidate
```

---

# 19. 复杂度自动升级规则

允许升级：

```text
tiny → normal → complex
```

不建议自动降级正在执行的 Workflow。

Tiny 升 Normal 示例：

- 修改范围超过预期
- 涉及多个模块
- 原因不是局部 bug
- 需要修改测试架构

常规工作流升级为复杂工作流：

- 模式迁移（schema migration）
- 公共 API 破坏性变更
- 重大架构决策
- 需要并行多个子任务
- 出现明显的跨模块依赖

升级时记录：

```text
工作流已从 normal 升级为 complex。

原因：
身份认证变更会影响 API 契约和数据库模式。
```

---

# 20. 恢复协议

每次：

```bash
workspace resume REQ-001
```

执行：

```text
1. 加载 meta.json

2. 加载 requirement.md

3. 加载 state.md

4. 加载 handoff.md

5. 加载 plan.md

6. 加载 decisions.md

7. 加载 verification.md

8. 查询 dashi 任务状态

9. 检查 Git 分支和工作树

10. 运行 git status

11. 检查最近的提交

12. 构建上下文快照

13. 确定下一步行动

14. 启动或恢复 Agent 会话
```

---

# 21. 上下文快照

Restore 后给 Agent 的上下文不要是所有文件原样拼接。

应生成精简 Snapshot：

```markdown
# 工作区上下文

需求：
REQ-001 身份认证

目标：
实现 JWT 身份认证。

当前阶段：
实现

当前任务：
TASK-003 认证中间件

已完成：
- 用户模型
- 登录接口

待处理：
- 中间件
- 集成测试

重要决策：
- JWT
- 认证逻辑保留在 auth 模块中

Git:
分支：feature/REQ-001-auth
工作树：.worktrees/REQ-001

验证：
单元测试 PASS
集成测试 TODO

上次交接：
登录接口已完成。

下一步行动：
实现认证中间件。
```

目标：

**让新 Thread 用最少 Token 恢复最关键上下文。**

---

# 22. 检查点

Checkpoint 不等于 Handoff。

Checkpoint 是当前 Thread 还可能继续，但需要持久化状态。

命令：

```bash
workspace checkpoint REQ-001
```

执行：

```text
update state.md
update plan.md
update verification.md
update dashi task
record current session
```

适合：

- 完成一个小阶段
- 即将开始新的 Task
- 长任务处理中

---

# 23. 交接

Handoff 表示：

```text
当前 Thread 可以结束
```

执行：

```text
checkpoint
+
generate handoff.md
+
record session
+
recommend next action
```

之后用户可以直接新开 Thread：

```bash
workspace resume REQ-001
```

---

# 24. dashi-taskboard 适配器

V1 不重新实现完整 Taskboard。

直接使用 dashi。

定义抽象接口：

```python
class TaskProvider:
    def create_task(...)
    def get_task(...)
    def update_status(...)
    def add_comment(...)
    def list_tasks(...)
    def link_session(...)
    def set_git_context(...)
```

实现：

```text
DashiTaskProvider
```

通过：

```text
taskctl
```

调用。

---

# 25. 任务数据模型

内部统一模型：

```python
Task(
    id,
    title,
    description,
    status,
    priority,
    parent_id,
    blocked_by,
    blocks,
    branch,
    worktree,
    session_ids,
)
```

这样未来可以：

```text
DashiTaskProvider
GitHubIssueProvider
LinearTaskProvider
```

---

# 26. Git 适配器

定义：

```python
class GitProvider:
    def status(...)
    def current_branch(...)
    def create_branch(...)
    def create_worktree(...)
    def recent_commits(...)
    def diff(...)
```

V1：

```text
LocalGitProvider
```

---

# 27. Codex 适配器

V1 只做 Codex。

不要一开始支持所有 Agent。

抽象：

```python
class AgentProvider:
    def build_context(...)
    def get_current_session_id(...)
    def record_session(...)
```

V1 的 Codex Adapter 可以先只负责：

- 获取 CODEX_THREAD_ID
- 构造 Workspace Context
- Session 记录

不要求 V1 自动启动 Codex。

因为 Skill 本身就在 Codex 里执行。

---

# 28. Skill（技能）设计

Skill 应该非常薄。

不要再复制 Superpowers 那种大量固定工作流规则。

Skill 主要负责：

```text
1. Detect workspace

2. Call restore

3. Read generated Context Snapshot

4. Follow selected workflow

5. Execute task

6. Update checkpoint

7. Generate handoff when needed
```

核心原则：

```text
The workspace system owns state.
The skill follows the workspace.
```

而不是：

```text
Skill owns all state.
```

---

# 29. Skill 的核心提示词原则

必须明确写：

```text
Default to the lightest workflow that can safely solve the task.

Do not perform architecture analysis for tiny fixes.

Do not create plans merely for process compliance.

Prefer direct execution when:
- the root cause is obvious,
- the blast radius is small,
- the expected change is local.

Escalate workflow only when evidence justifies it.
```

这条非常重要。

---

# 30. 命令行界面（CLI）V1

V1 只实现下面几个命令。

## 创建工作区

```bash
workspace new "实现身份认证"
```

输出：

```text
已创建 REQ-001
```

---

## 查看状态

```bash
workspace status REQ-001
```

---

## 恢复

```bash
workspace resume REQ-001
```

输出 Context Snapshot。

---

## 检查点

```bash
workspace checkpoint REQ-001
```

---

## 交接

```bash
workspace handoff REQ-001
```

---

## 审查

```bash
workspace review REQ-001
```

检查：

- 验收标准（Acceptance Criteria）
- 任务状态（Task Status）
- 验证（Verification）

然后进入：

```text
in_review
```

---

# 31. 生命周期

需求：

```text
draft（草稿）
↓
ready（就绪）
↓
in_progress（进行中）
↓
in_review（审查中）
↓
done（已完成）
```

只有用户明确确认后：

```text
in_review（审查中）→ done（已完成）
```

Agent 不允许自主 done。

---

# 32. 任务生命周期

```text
todo（待处理）
↓
in_progress（进行中）
↓
in_review（审查中）
↓
done（已完成）
```

已阻塞：

```text
in_progress（进行中）→ blocked（已阻塞）
```

---

# 33. V1 不做什么

这一节非常重要。

V1 明确不做：

```text
❌ Web UI

❌ 桌面应用

❌ Agent 仪表板

❌ Multica 集成

❌ Claude Code 集成

❌ Cursor 集成

❌ Obsidian 自动写入

❌ 向量数据库

❌ 嵌入（Embedding）

❌ RAG

❌ 自主 Agent 集群

❌ 自动 PR Merge

❌ 自动 CI 修复

❌ 多 Agent 调度

❌ 复杂 workflow DSL

❌ 云同步

❌ 自定义数据库
```

V1 只证明核心模型：

```text
Persistent Requirement
+
Task State
+
Disposable Session
+
Restore / Checkpoint / Handoff
+
Adaptive Workflow
```

---

# 34. V1 最关键的演示

最终必须能够完整演示：

```text
用户：
创建需求：增加用户认证
```

系统：

```text
已创建 REQ-001
```

创建 dashi Tasks：

```text
TASK-001 登录
TASK-002 中间件
TASK-003 测试
```

Codex 线程 A：

```text
workspace resume REQ-001
```

得到 Context Snapshot。

执行 TASK-001。

然后：

```text
workspace handoff REQ-001
```

结束 Thread A。

---

新建 Codex Thread B。

只输入：

```text
继续 REQ-001
```

Skill 自动：

```text
workspace resume REQ-001
```

恢复：

```text
登录已完成
当前任务：中间件
下一步行动：实现中间件
```

Thread B 不需要知道 Thread A 完整历史。

继续开发。

这就是 V1 成功的核心验收标准。

---

# 35. V1 验收标准

## 需求工作区

- [ ] 可以创建 Requirement
- [ ] Requirement 有稳定 ID
- [ ] Workspace 文件自动初始化

## 任务

- [ ] 可以通过 dashi 创建 Task
- [ ] Task 与 Requirement 建立关联
- [ ] 可以更新状态

## 会话

- [ ] 能记录 CODEX_THREAD_ID
- [ ] 一个 Requirement 支持多个 Session
- [ ] Session 切换不会丢失 Requirement 状态

## 恢复

- [ ] 新 Thread 可以恢复 Requirement
- [ ] 可以读取 handoff
- [ ] 可以读取 Git 状态
- [ ] 可以读取 Task 状态
- [ ] 自动生成 Context Snapshot

## 工作流

- [ ] 支持 tiny
- [ ] 支持 normal
- [ ] 支持 complex
- [ ] 支持 research
- [ ] tiny 默认不进行长规划
- [ ] 可以根据证据升级 workflow

## 检查点

- [ ] 更新 state
- [ ] 更新 plan
- [ ] 更新 verification

## 交接

- [ ] 自动生成 handoff.md
- [ ] 记录完成内容
- [ ] 记录下一步
- [ ] 记录重要上下文

## 审查

- [ ] 检查 Acceptance Criteria
- [ ] 检查 Verification
- [ ] Agent 不能直接标记 Requirement done

---

# 36. 推荐开发顺序

不要一次实现全部。

## 阶段 1

实现：

```text
Workspace
Requirement
File templates
CLI
```

完成：

```text
workspace new
workspace status
```

---

## 阶段 2

实现：

```text
Restore
Checkpoint
Handoff
Context Snapshot
```

完成：

```text
workspace resume
workspace checkpoint
workspace handoff
```

这是整个项目最关键阶段。

---

## 阶段 3

接入：

```text
dashi-taskboard
Git
```

---

## 阶段 4

实现：

```text
Complexity Router
Tiny Workflow
Normal Workflow
Complex Workflow
Research Workflow
```

---

## 阶段 5

制作：

```text
Codex Skill
```

让用户可以自然输入：

```text
继续 REQ-001
```

而不是每次手工调用 CLI。

---

# 37. 核心工程原则

## 原则 1

Do not store knowledge only in conversation history.

## 原则 2

Do not force full planning on simple work.

## 原则 3

Prefer execution over ceremony.

## 原则 4

Persistent state must be human-readable.

因此优先：

```text
Markdown + JSON
```

而不是数据库。

## 原则 5

Adapters must isolate external tools.

Core 不允许直接依赖：

```text
taskctl
codex
obsidian
multica
```

必须通过 Adapter。

## 原则 6

V1 must remain local-first.

## 原则 7

Every session must be replaceable.

如果一个 Thread 无法被替换，则说明持久状态设计失败。

---

# 38. 后续架构

未来可以扩展：

```text
任务 Provider
├── Dashi
├── GitHub Issues
└── Linear

Agent Provider
├── Codex
├── Claude Code
├── Cursor
├── OpenCode
└── Multica

知识 Provider
├── Markdown
├── Obsidian
└── 向量存储

执行环境
├── 本地
├── 工作树
├── 容器
└── 远程运行时
```

但这些都不属于 V1。

---

# 39. 项目核心口号

Requirements persist.

Tasks evolve.

Sessions are disposable.

Knowledge compounds.

Process scales with complexity.

中文：

需求持久存在。

任务持续演进。

会话随时可丢。

知识不断积累。

流程复杂度随任务复杂度增长。

---

# 40. 给 Codex 的开发要求

实现时必须遵守：

1. 不要超出 V1 Scope。
2. 不要提前实现 Web UI。
3. 不要添加数据库。
4. 不要实现复杂 Agent Framework。
5. 不要复制 Superpowers 的重型流程。
6. 优先完成 Restore / Checkpoint / Handoff。
7. 所有外部工具必须通过 Adapter。
8. Core 必须可以在没有 dashi 的情况下运行。
9. dashi Adapter 是可选集成。
10. 所有 Workspace 文件必须人类可读。
11. 必须为核心生命周期写测试。
12. 对 tiny task 优先直接执行，不允许无意义长规划。
13. 每完成一个 Phase 后先验证，再进入下一 Phase。
14. 如果发现需求与本架构冲突，不要自行扩大 Scope，记录问题并继续保持 V1 最小实现。

---

# 41. Automation First / Deterministic Runtime

执行优先级：

1. Deterministic code
2. Rule-based automation
3. Small semantic decision
4. Full Agent reasoning

凡是输入、规则和输出确定的流程，必须由 `workspace_orchestrator.automation` 中的普通 Python 代码执行。Automation Layer 不调用 LLM、不依赖对话历史、相同输入产生相同选择、操作尽量幂等并可单元测试。

Codex 仓库级 Hook 是启动边界：`SessionStart`、`UserPromptSubmit` 和 `SessionEnd` 只负责把结构化事件交给 Automation Runtime。Runtime 自动处理 Session、Requirement、Task、dashi、Git、Snapshot、verification、checkpoint、handoff 与 detach。Skill 仅作为不支持或未信任 Hook 时的一次触发回退。

AI 只负责语义理解、歧义判断、Root Cause 分析、架构与实现决策、代码生成/修改、Intent 判断和无法用确定规则解决的选择。Requirement 仍是高层意图对象，不因小修改自动创建，也不随 Task `done` 自动完成。
